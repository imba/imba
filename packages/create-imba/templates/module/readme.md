# Module Template

A module written in Imba that can be published to npm and used in any JavaScript project.

- `npm run dev` — develop against the demo page in `index.html`
- `npm run build` — build the module to `dist/`

See https://imba.io/ for documentation.
