const arr\any[] = []

test do
	let item\string = "hello"
	let str2 = (window.title)\string
	let str3 = arr[1]\string
	let one\One

class One
	refs\(string | number)[]

	def constructor
		sources\One[]
		files\File[] = []

		if let res\number = Hello.test!
			yes

	def method param\string, callback\Function
		callback(10,20)
		yes

	def destruct {a,b,c} = {a:1,b:2,c:3}
		yes

	def filter
		sources.filter do(item\One)
			item.refs.length > 1
			item.refs[3].toUpperCase!

	def setup
		for item in window.stuff\string[]
			item.toUpperCase!
		return