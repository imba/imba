{
    "name": "imba-monorepo",
    "private": true,
    "workspaces": [
        "packages/imba-language-core",
        "packages/imba-language-server",
        "packages/imba-typescript-plugin",
        "packages/vscode-imba-next"
    ],
    "devDependencies": {
        "lerna": "^9.0.7",
        "@types/node": "^22.0.0",
        "typescript": "^5.8.0",
        "vitest": "^3.0.0"
    },
    "scripts": {
        "deploy-site": "ssh imba@imba.io \"bash ~/deploy-imba.io.sh\"",
        "release": "lerna publish",
        "prereleasez": "lerna publish --force-publish --canary --preid next-$(git log -1 --format=%at) --dist-tag next --pre-dist-tag next"
    }
}
