{
    "name": "imba-monorepo",
    "private": true,
    "devDependencies": {
        "lerna": "^8.0.0"
    },
    "scripts": {
        "release": "lerna publish",
        "prerelease": "lerna publish --force-publish --canary --preid next-$(git log -1 --format=%at) --dist-tag next --pre-dist-tag next"
    }
}
